[project]
name = "skill-auto"
version = "0.1.0"
description = "Automated Skill testing and onboarding framework for LazyMind."
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[project.scripts]
skill-auto = "skill_auto.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
